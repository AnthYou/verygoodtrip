
<div class="container mt-5">
  <h1 class="text-center"> Find your trip </h1>
  <%= render 'layouts/tripcards' %>

  <div class="row p-1">
      <%= link_to 'Add new destination', new_trip_path(@trip), class: 'btn btn-outline-primary' %>
  </div>
</div>
